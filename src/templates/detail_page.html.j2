<article class="page-block">
  {% if material %}
  {{ material }}
  {% else %}
  <h2>{{ title }}</h2>
  <p class="empty">
    No material selected. Open a record from <a href="{{ page.relbaseurl }}/search">search results</a>.
  </p>
  {% endif %}
</article>
