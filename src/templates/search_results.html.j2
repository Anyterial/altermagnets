{% if result and result|length > 0 %}
<table class="results-table" aria-label="Search results table">
  <thead>
    <tr>
      <th>Formula</th>
      <th>Material</th>
      <th>Space group</th>
      <th>Order</th>
    </tr>
  </thead>
  <tbody>
    {% for item in result %}
    <tr class="result-row" data-row-url="{{ page.relbaseurl }}/material?id={{ item.id }}" tabindex="0">
      <td>{{ item.formula }}</td>
      <td>{{ item.name }}</td>
      <td>{{ item.spacegroup }}</td>
      <td>{{ item.order }}</td>
    </tr>
    {% endfor %}
  </tbody>
</table>
{% else %}
<p class="empty">No matches found for this query.</p>
{% endif %}
