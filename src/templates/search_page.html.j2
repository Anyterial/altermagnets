<article class="page-block">
  <header class="hero-block compact">
    <h2>{{ title }}</h2>
    <p class="search-note">
      Search is executed in the browser against the screened materials OPTIMADE service.
    </p>
  </header>
  <!-- Loaded before the table widget so the on-load filter normalizer runs first (no unfiltered flash). -->
  <script src="{{ page.relbaseurl }}/search-form.js" defer></script>
  {{ content }}
</article>
