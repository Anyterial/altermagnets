[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "example_website_httk"
version = "0.1.0"
description = "Example of how to use httk as a static site generator (that can be extended with a backend)."
readme = "README.md"
license = "AGPL-3.0+"
requires-python = ">=3.12"
authors = [{ name = "Rickard Armiento", email = "gitcommits@armiento.net" }]
dependencies = [
  "httk-data[duckdb]>=0.2,<1",
  "httk-serve>=0.2,<1",
]
classifiers = [
  "Operating System :: OS Independent",
  "Programming Language :: Python",
]
[project.optional-dependencies]
dev = [
  "pytest>=8.0",
  "httpx>=0.27",
  "black",
  "ruff",
  "isort",
  "pyright",
  "mypy"
]
detail-generation = [
  "matplotlib",
  "pymatgen",
  "ase",
]
optimade = [
  "httk-io>=0.1,<1",
  "httk-atomistic>=0.3,<1",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.isort]
profile = "black"
extend_skip_glob = ["src/data/**"]

[tool.ruff]
target-version = "py312"
src = ["src"]
extend-exclude = ["src/data"]
force-exclude = true

[tool.black]
target-version = ["py312"]
line-length = 120
skip-string-normalization = true
force-exclude = '''
/src/data/
'''

[tool.pyright]
pythonVersion = "3.12"
include = ["src/functions", "tools/build_store.py", "serve_dynamic.py", "serve_combined.py", "serve_optimade.py", "publish_static.py", "optimade/render_definitions.py"]
exclude = ["src/data"]
extraPaths = ["src", "src/functions"]
reportMissingImports = true

[tool.mypy]
python_version = "3.12"
files = ["src/functions", "tools/build_store.py", "serve_dynamic.py", "serve_combined.py", "serve_optimade.py", "publish_static.py", "optimade/render_definitions.py"]
mypy_path = ["src", "src/functions"]
namespace_packages = true
explicit_package_bases = true
warn_unused_ignores = true
exclude = ['^src/data/']

[[tool.mypy.overrides]]
 module = ["yaml", "markdown", "docutils", "docutils.*"]
ignore_missing_imports = true
